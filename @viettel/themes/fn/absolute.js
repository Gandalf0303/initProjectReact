export default {
  top: value => ({
    top: Number(value)
  }),
  left: value => ({
    left: Number(value)
  }),
  right: value => ({
    right: Number(value)
  }),
  bottom: value => ({
    bottom: Number(value)
  })
};
