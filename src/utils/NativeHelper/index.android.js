import { NativeModules } from 'react-native';

const { RNUtilities } = NativeModules;

/**
 appVersion
 buildVersion
 bundleIdentifier
 locale
 country
 countryCode
 share
 */
export default RNUtilities;
